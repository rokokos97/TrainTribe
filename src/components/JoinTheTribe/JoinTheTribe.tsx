import React from "react";
import styles from "./JoinTheTribe.module.scss";

const JoinTheTribe: React.FC = () => {
  return (
    <section className={styles.JoinTheTribe}>
      Here will be form to propose a new training session.
    </section>
  );
};

export default JoinTheTribe;
